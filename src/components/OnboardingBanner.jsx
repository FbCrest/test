import { useState, useEffect, useRef } from 'react';

/**
 * Onboarding Banner component - DISABLED for Vercel deployment
 * The onboarding banner has been removed to provide immediate access to the app
 */
const OnboardingBanner = () => {
  // Always return null - banner is disabled
  return null;
};

export default OnboardingBanner;
